apiVersion: v1
kind: Service
metadata:
  name: {{ include "dummy-website.fullname" . }}
  labels:
    {{- include "dummy-website.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dummy-website.selectorLabels" . | nindent 4 }} 